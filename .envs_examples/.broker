# RabbitMQ + Redis (Celery) — только worker (не подключать к core-api в docker-compose)
CELERY_BROKER_URL=amqp://guest:guest@localhost:5672//
CELERY_RESULT_BACKEND=django-db
REDIS_URL=redis://localhost:6379/0
